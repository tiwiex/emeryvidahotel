{% if content.field_layout.0['#markup'] in 'style 2' %}
<section class="services services-with-icon backgound">
    <div class="container">
       <div class="tag-title">
            <span class="tagline">{{content.field_tag_line.0}}</span>
            <h2 class="sec-title">{{content.field_title.0}}</h2>
        </div>  
        {{content.field_services_grid_with_icon}}
    </div>
</section>
{% else %}
<section class="services services-with-icon">
    <div class="container">
       <div class="tag-title">
            <span class="tagline">{{content.field_tag_line.0}}</span>
            <h2 class="sec-title">{{content.field_title.0}}</h2>
        </div>  
        {{content.field_services_grid_with_icon}}
    </div>
</section>
{% endif %}