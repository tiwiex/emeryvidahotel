<section class="about-us about-style-3">
    <div class="container">
        <div class="row">
            {# <div class="about-wrapper"> #}
                <div class="col-md-7 img">
                    <img src="{{file_url(content.field_image['#items'].entity.uri.value)}}" alt="{{file_url(content.field_image['#items'].alt)}}">
                </div>
                <div class="col-md-5">
                    <div class="content">
                        <h2>{{content.field_title.0}}</h2>
                        {{content.field_description.0}}
                        <div class="btn-link">
                            <a href="{{cotent.field_button_link.0['#url']}}">{{content.field_button_link.0['#title']}}</a>
                        </div>    
                    </div>
                </div>
            {# </div>     #}
        </div>
    </div>
</section>            